# CLAUDE_BOOTSTRAP.md (Generated)

> **Generated:** 2026-07-09 09:59:55 UTC
> **Authority:** synthesized from `.context/*.generated.*` — see `scripts/generate-claude-bootstrap.mjs`.
> This is the canonical entry point for a new Claude/GPT/Cursor agent working on BrandOS. It is a
> *synthesis*, not a replacement — every section cites the generated artifact it was derived from so
> you can go straight to the source for more detail.
> ⚠️ Do not edit by hand — regenerated by `node scripts/generate-claude-bootstrap.mjs`

---

## Overview

- **Packages:** 16 libraries + 1 application = 17 total
- **Architectural layers:** 12 (L0–L11)
- **Critical packages:** `@brandos/contracts`, `@brandos/control-plane-layer`, `@brandos/shared-utils`, `@platform/cognition-contract`
- **Behavior contracts tracked:** 6
- **Generated:** 2026-07-09 09:59:55 UTC

This document is itself generated — see `scripts/generate-claude-bootstrap.mjs`. It synthesizes the architecture-intelligence layer below; it does not replace any single file in it.

## Required Reading Order

Read in this order. Each entry is checked for existence at generation time — a missing file means the context-generation pipeline has not been run, not that the step should be skipped.

1. ✅ `.context/monorepo_context.generated.md` (monorepo_context)
2. ✅ `.context/system_inventory.generated.md` (system_inventory)
3. ✅ `.context/runtime_model.generated.md` (runtime_model)
4. ✅ `.context/runtime_trace.generated.md` (runtime_trace)
5. ✅ `.context/architecture_graph.generated.json` (architecture_graph)
6. ✅ `.context/dependency_impact.generated.json` (dependency_impact)
7. ✅ `.context/behavior_contracts.generated.json` (behavior_contracts)
8. ✅ `.context/agent_entrypoints.generated.md` (agent_entrypoints)
9. ✅ `.context/packages/<package>.generated.md` — package-specific context (17 available; read the one(s) you're about to touch)

## Runtime Flow

Extracted from `runtime_trace.generated.md`, which is itself generated by reading source live — this is the actual call graph, not a description of intent.

- **3** route(s) run the full pipeline: `runControlPlane()` then `executeArtifactPipeline()` (two sequential top-level calls, not nested).
- **1** route(s) run free-text generation only (`runControlPlane()`, governed by `evaluateGovernance()` inline, no structured artifact pipeline).
- Prompt assembly runs **6 contributors** per request via `ContractAssemblerFactory` (no startup singleton — assembled fresh every call).
- Governance repair loop ceiling: **3 attempts** (`MAX_REPAIR_ATTEMPTS`) before a structured artifact fails outright.

**Where the live trace disagrees with other docs** (see `runtime_trace.generated.md` §8 for full citations):

1. **`executeArtifactPipeline()` is a second top-level call from the route, not a nested step inside `CPLOrchestrator.orchestrate()`.** See §2.
2. **`ContractAssemblerFactory` registers 6 contributors** (IdentityContributor, PersonaContributor, IntentContributor, ArtifactContributor, RuntimeContributor, SkillContributor), not the 2 most other context docs mention by name. See §4.
3. **`MAX_REPAIR_ATTEMPTS = 3`** in the live code (comment: _Raised from 2 → 3 to match raised governance thresholds (quality initiative). Further increases require explicit cost/latency analysis._). See §3.
4. **`campaigns` writes happen directly from `apps/web` route handlers**, not through any `@brandos/auth` exported function, despite `@brandos/auth` being the documented table owner. See §7.
5. **The AI-runtime config provider bridge is wired lazily** on first admin-settings load, not during the `instrumentation.ts` boot sequence. See §1.

## Critical Packages

- **Highest consumer count:** `@platform/cognition-contract` — 14 package(s) transitively depend on it.
- **Highest blast radius:** `@platform/cognition-contract` — 14 package(s) would be affected by a breaking change.

**Risk level `critical`** (see `dependency_impact.generated.json` `_meta.methodology` for the exact formula):

| Package | Score | Transitive consumers | Direct deps | Routing chokepoint |
|---|---|---|---|---|
| `@brandos/contracts` | 27 | 13 | 1 | no |
| `@brandos/control-plane-layer` | 24 | 1 | 12 | yes |
| `@brandos/shared-utils` | 21 | 10 | 1 | no |
| `@platform/cognition-contract` | 28 | 14 | 0 | no |

## Top 10 High-Risk Files

Derived by combining three independent generated signals: the owning package's blast-radius score (`dependency_impact.generated.json`), whether the file sits on a live execution path (`runtime_trace.generated.md`), and whether the file is named in an explicit architectural restriction (`architecture_graph.generated.json` + `agent_entrypoints.generated.md`, the latter itself sourced from each package's `AGENT_CONTEXT.md`).

`score = ownerBlastRadiusScore + 10 × (number of independent sources mentioning the file) + 25 if the file is named in a hard "no-touch" restriction`

| # | File | Owner | Owner risk | Sources | No-touch | Score |
|---|---|---|---|---|---|---|
| 1 | `packages\artifact-engine-layer\src\engine.ts` | _unresolved_ | unknown | architectural_restrictions, runtime_trace | ⚠️ yes | 45 |
| 2 | `packages\artifact-engine-layer\src\registry.ts` | _unresolved_ | unknown | architectural_restrictions, runtime_trace | ⚠️ yes | 45 |
| 3 | `packages/control-plane-layer/src/admin/settings-service-supabase.ts` | @brandos/control-plane-layer | critical | runtime_trace | no | 34 |
| 4 | `apps/web/app/layout.tsx` | @brandos/web | high | architectural_restrictions | no | 25 |
| 5 | `apps/web/instrumentation.ts` | @brandos/web | high | architectural_restrictions | no | 25 |
| 6 | `apps/web/lib/pl-auth-bridge.tsx` | @brandos/web | high | architectural_restrictions | no | 25 |
| 7 | `packages/artifact-engine-layer/src/bootstrap.ts` | @brandos/artifact-engine-layer | medium | runtime_trace | no | 17 |
| 8 | `packages/cognition-client/src/index.ts` | @brandos/cognition-client | medium | architectural_restrictions | no | 16 |
| 9 | `packages\ai-runtime-layer\src\AIRuntimeAdapter.ts` | _unresolved_ | unknown | architectural_restrictions | no | 10 |
| 10 | `packages\ai-runtime-layer\src\llmRouter.ts` | _unresolved_ | unknown | architectural_restrictions | no | 10 |

## Package Ownership Summary

One-line purpose per package, extracted from the first sentence of each package's own `AGENT_CONTEXT.md` "Package Purpose" section (embedded in `.context/packages/<pkg>.generated.md`).

| Package | Layer | Purpose |
|---|---|---|
| `@platform/cognition-contract` | L0 | _(no Package Purpose section found)_ |
| `@brandos/contracts` | L1 | Zero-dependency canonical type kernel for the entire BrandOS platform. |
| `@brandos/shared-utils` | L2 | Infrastructure primitive layer. |
| `@brandos/cognition-client` | L3 | The single adapter boundary between BrandOS and IntelligenceOS. |
| `@brandos/auth` | L4 | `@brandos/auth` is the single source of truth for all authentication operations and database CRUD. |
| `@brandos/runtime-config` | L5 | Single source of truth for AI **runtime** configuration. |
| `@brandos/governance-config` | L5 | Single source of truth for governance, compliance, and quality **policy configuration**. |
| `@brandos/artifact-config` | L5 | Single source of truth for **artifact engine configuration**. |
| `@brandos/ui-admin` | L5 | Shared admin UI primitives. |
| `@brandos/ai-runtime-layer` | L6 | `@brandos/ai-runtime-layer` is the **sole AI execution engine** for the BrandOS platform. |
| `@brandos/output-control-layer` | L6 | Stateless, deterministic hybrid compiler. |
| `@brandos/governance-layer` | L7 | Semantic governance authority for BrandOS artifact generation. |
| `@brandos/iskill-runtime` | L7 | Governed execution runtime for ISkill and ICP Bundle orchestration. |
| `@brandos/artifact-engine-layer` | L8 | Horizontal artifact orchestration runtime. |
| `@brandos/control-plane-layer` | L9 | Single integration kernel for the BrandOS platform. |
| `@brandos/presentation-layer` | L10 | Pure React artifact renderers and workspace shells. |
| `@brandos/web` | L11 | The deployed BrandOS web application. |

## Architectural Rules

Highest-value restrictions, selected automatically from `monorepo_context.generated.md` by how many `critical`/`high` risk packages (per `dependency_impact.generated.json`) each rule concerns — not a fixed editorial list. Full rule set: `monorepo_context.generated.md` § Architectural Rules.

- **`RULE-3 — CPL cognition-client symbol allowlist`** — `@brandos/control-plane-layer` may only import specific symbols from `@brandos/cognition-client`.
- **`RULE-OCL-GOVERNANCE-CONFIG`** — `@brandos/output-control-layer` must NOT import from `@brandos/governance-config`.
- **`RULE-5 — GL ↛ OCL (Fix C2)`** — `@brandos/governance-layer` must NOT import from `@brandos/output-control-layer`.
- **`RULE-6 — retired, folded into RULE-3 (v6)`** — (Retired) Formerly: `@brandos/control-plane-layer` must NOT import concrete BI repository classes. Under the current HTTP-adapter architecture, IntelligenceOS's repository layer is not visible to BrandOS at all — this concern has no analog. See RULE-3.
- **`RULE-7 — retired, folded into RULE-3 (v6)`** — (Retired) Formerly: `@brandos/control-plane-layer` must NOT import BrandIntelligenceRuntime concrete class as a value. Current equivalent: CPL must not import HttpCognitionProvider/DegradedCognitionProvider as values — use createDegradedCognitionContext() / getGlobalCognitionClient() instead. Enforced as part of RULE-3's allowlist, not a separate check.

**Derived (not a named rule, computed from the dependency graph):**
- `@platform/cognition-contract` depends on nothing and is depended on by 14 other package(s) — the canonical authority for whatever it exports.

## Agent Workflow

1. Read this document.
2. Read `.context/packages/<package>.generated.md` for the package you are about to touch.
3. Read `.context/dependency_impact.generated.json` for that package — know the blast radius before changing anything.
4. Read `.context/behavior_contracts.generated.json` for any cross-package contract your change affects.
5. Implement the change.
6. Validate: `node scripts/check-boundaries.mjs`, `node scripts/check-workspace.mjs`, and review `.context/architecture_fixes.generated.md` for any newly introduced violation.
7. Run `pnpm context:refresh` so `.context/` and this document reflect the change for the next agent.